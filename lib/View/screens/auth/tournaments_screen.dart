import 'package:flutter/material.dart';

class TournamentsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Tournaments Screen"),
    );
  }
}
